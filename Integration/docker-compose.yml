version: "3.4"

services:
  api:
    image: my-registry:55000/api:ci-10
    # ports:
    #   - "8080:80"
  mail:
    image: mailhog/mailhog
    # ports:
    #   - "8025:8025"
  integration:
    image: my-registry:55000/api:integration-10
    build: .
